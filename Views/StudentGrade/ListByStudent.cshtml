
@{
    ViewBag.Title = "ListByStudent";
}

@{
    ViewBag.Title = "ListByStudent";
}

@section title { Grades List By Student }
@section breadcrumb{ <a href="/StudentGrade/ListByCourse">ListByStudent</a>}

@model List<StundetInformation.Models.StudentGrade>

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        @if (Model.Count == 0)
                        {
                            <h3 class="float-left">No Grades Found</h3>
                        }
                        else
                        {
                            <h3 class="float-left">@Model[0].Student.firstName @Model[0].Student.lastName</h3>
                        }
                        <a class="btn btn-primary float-right ml-3 mr-3" href="/StudentGrade/AddGradeToStudent/@ViewBag.StudentID" role="button">Add Grade</a>
                    </div>
                    <!-- /.card-header -->
                    <div class="card-body">
                        <table id="gradesList" class="table table-bordered table-striped">
                            <thead>
                                <tr>
                                    <th>Course</th>
                                    <th>Grade</th>
                                    <th>Grade Weight</th>
                                    <th>Date</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var grade in Model)
                                {
                                    <tr>
                                        <td>@grade.Course.courseName</td>
                                        <td>@grade.gradeValue</td>
                                        <td>@grade.gradeWeight</td>
                                        <td>@grade.date</td>
                                        <td>
                                            <a href="/StudentGrade/EditGradeFromStudent/@grade.id"><i class="fa-solid fa-pen-to-square ml-3 mr-3"></i></a>
                                            <a href="/StudentGrade/RemoveGradeFromStudent/@grade.id"><i class="fa-solid fa-trash ml-3 mr-3"></i></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                            <tfoot>
                                <tr>
                                    <th>Course</th>
                                    <th>Grade</th>
                                    <th>Grade Weight</th>
                                    <th>Date</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
</section>




